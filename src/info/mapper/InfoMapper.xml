<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="info.mapper.InfoMapper">

	<!-- 연도별 현황 가져오기  -->
	<select id="selectByYears" parameterType="string"
		resultType="Info">
		select from condition where years=#{value} 
	</select>

</mapper>
